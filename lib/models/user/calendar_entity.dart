class MonthEntity {
  String month;
  bool isSelected;
  MonthEntity({
    required this.month,
    required this.isSelected,
  });
}

class YearEntity {
  int year;
  bool isSelected;
  YearEntity({
    required this.year,
    required this.isSelected,
  });
}
